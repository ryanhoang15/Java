public int countX(String str) {
  if (str.equals("")){
    return 0;
  } 

  if (str.charAt(0) == 'x') {
    return countX(str.substring(1)) + 1;
  }

  else {
    return countX(str.substring(1));
  }
  
}
