public int[] biggerTwo(int[] a, int[] b) {
  int a1 = a[0] + a[1];
  int b1 = b[0] + b[1];
  
  if(a.length == 2 && b.length == 2){
    if(a1 > b1 || a1 == b1){
      return a;
    }
  }
  return b;
}
